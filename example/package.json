{
  "name": "thermalib-expo-example",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "adb:connect": "adb reverse tcp:8081 tcp:5555",
    "start": "expo start --dev-client -c",
    "start:prod": "expo start --no-dev",
    "android": "expo run:android",
    "android:release": "expo run:android --variant release",
    "expo:fix": "npx expo install --check && npx expo install --fix",
    "ios": "npm run prebuild && npm run pods && expo run:ios --device",
    "web": "expo start --web",
    "pods": "rm -rf ios/Pods ios/Podfile.lock && npx pod-install",
    "prebuild": "npm i && npx expo prebuild",
    "prebuild:debug": "EXPO_DEBUG=1 npx expo prebuild",
    "prebuild:clean": "npm i && npx expo prebuild --clean",
    "build": "rm -rf android && rm -rf ios && npm run prebuild:clean"
  },
  "dependencies": {
    "@mobione/thermalib-expo": "../",
    "expo": "^55.0.6",
    "expo-location": "~55.1.4",
    "react": "19.2.0",
    "react-native": "0.83.2"
  },
  "devDependencies": {
    "@babel/core": "^7.25.2",
    "@types/react": "~19.2.10",
    "eslint": "^8.57.1",
    "typescript": "~5.9.2"
  },
  "private": true,
  "expo": {
    "autolinking": {
      "nativeModulesDir": ".."
    }
  }
}
